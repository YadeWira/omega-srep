# Omega SREP `-dup` benchmark (F5.4)

Generated by `tests/dup_bench.sh` on 2026-04-27.

## Setup

- Host: `Linux 6.12.74+deb13+1-amd64 x86_64`, 4 cores, 7.0 GiB RAM
- Method: `-m4`
- CDC buffer size: 8 MiB (`--buf=8388608`)
- Block content: 1 MiB text + 31 MiB xorshift64 binary, repeated identically

RSS measurements use `tests/measure.py` (a Python wrapper around
`getrusage(RUSAGE_CHILDREN)`, since GNU `time` isn't installed on
this host).

The F5.3a / F5.3b sections below cover **only the `osrep` invocation
that compresses or decompresses the unique-chunk body** — i.e. they
isolate SREP's working set. The dedup pre-pass and post-pass that
sit on either side are excluded from the RSS column because the
current implementation reads the full input into a contiguous
`std::vector` buffer (a known limitation; see the "Native end-to-end
RSS" section below and task **F5.3c** for the streaming follow-up
that would close it).

## Results — 128 MiB corpus (32 MiB block × 4)

| metric                       | A (`-m4`) | B (`-m4 -dup`) | delta |
|------------------------------|----------:|---------------:|------:|
| archive size (bytes)         | 33498032  | 33591092       | 100.3% of A |
| compress peak RSS (KiB)      | 21608     | 18664          | -13.6% |
| decompress peak RSS (KiB)    | 53480     | 17512          | **-67.3%** |
| compress wall-clock (s)      | 0.46      | 0.17           |        |
| decompress wall-clock (s)    | 0.38      | 0.08           |        |

Pipeline structure (B): meta=93720 B, body=33554432 B (25% of input),
body.osr=33497360 B, final archive=33591092 B.

## Results — 256 MiB corpus (32 MiB block × 8)

| metric                       | A (`-m4`) | B (`-m4 -dup`) | delta |
|------------------------------|----------:|---------------:|------:|
| archive size (bytes)         | 33498928  | 33671220       | 100.5% of A |
| compress peak RSS (KiB)      | 25832     | 18664          | -27.7% |
| decompress peak RSS (KiB)    | 53480     | 17512          | **-67.3%** |
| compress wall-clock (s)      | 0.87      | 0.17           |        |
| decompress wall-clock (s)    | 0.70      | 0.08           |        |

Pipeline structure (B): meta=173848 B, body=33554432 B (12% of input),
body.osr=33497360 B, final archive=33671220 B.

## Trend

| corpus  | A compress RSS | B compress RSS | A decompress RSS | B decompress RSS |
|--------:|---------------:|---------------:|-----------------:|-----------------:|
| 128 MiB | 21608 KiB      | 18664 KiB      | 53480 KiB        | 17512 KiB        |
| 256 MiB | 25832 KiB      | 18664 KiB      | 53480 KiB        | 17512 KiB        |

B's RSS is flat across input size because SREP only ever sees the
unique-chunk body (32 MiB in both runs). A's compress RSS grows with
input; A's decompress RSS plateaus at the SREP buffer ceiling
(`-b8mb` plus per-buffer state). Wall-clock for A scales linearly
with input; B is constant. Larger or more redundant corpora widen
the gap.

## Native end-to-end RSS (F5.3b)

The F5.3a "B" rows above measure SREP's working set in isolation —
they show what happens when SREP only ever sees the unique-chunk
body. The native `-dup` flag delivered by F5.3b runs the same
pipeline inside one `osrep` process, so its RSS includes the dedup
buffer:

| metric                    | A (`-m4`) | B (`osrep -dup -m4`, end-to-end) |
|---------------------------|----------:|---------------------------------:|
| compress peak RSS (KiB)   | 21772     | 198196                           |
| decompress peak RSS (KiB) | 53516     | 214924                           |
| archive size (bytes)      | 33498032  | 33591092 (byte-identical to F5.3a) |
| compress wall-clock (s)   | 0.45      | 0.98                             |
| decompress wall-clock (s) | 0.38      | 0.42                             |

So today the native `-dup` path costs ≈ `2 × input_size + small` RAM
because `dup_wrapper.cpp` calls `dedup::encode_split` on a
fully-loaded input buffer and writes the body to a tempfile in one
shot. Round-trip and archive bytes are correct; only RSS is the
limiter. This is the gap **F5.3c** addresses: rewrite the dedup
pre/post-pass to stream buffer-by-buffer (we already have a
`buf_size` knob for CDC), at which point native `-dup` should match
the F5.3a SREP-only column above.

## Success criterion

Design target (per `docs/dup-mode-design.md`): B's peak decompress
RSS < A's by ≥50% with archive bloat ≤10%.

- SREP-on-body decompress peak RSS reduction: **-67.3%** (target ≥50%) ✓
- archive size: **100.3-100.5% of A** (target ≤110%) ✓
- end-to-end native peak RSS (today, with full-buffer dedup): worse
  than A on this corpus; F5.3c's streaming refactor is required to
  realize the SREP-on-body savings in production.

The F5 thesis is validated for SREP's working set. The F5.3a
pipeline already realizes the savings end-to-end (its dedup steps
are separate processes that the bench script intentionally excludes
from `getrusage`); F5.3b's native integration unifies the UX but
defers the streaming refactor.

## Reproduction

```
make bin/osrep bin/dedup_test
bash tests/dup_bench.sh                          # 128 MiB run
OSREP_BENCH_REPEATS=8 \
OSREP_BENCH_OUT=docs/dup-bench-256.md \
bash tests/dup_bench.sh                          # 256 MiB run
```

Tunables (env vars): `OSREP_BENCH_BLOCK_MB`, `OSREP_BENCH_REPEATS`,
`OSREP_BENCH_METHOD`, `OSREP_BENCH_BUF`, `OSREP_BENCH_DIR`,
`OSREP_BENCH_OUT`.
