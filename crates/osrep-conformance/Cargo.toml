[package]
name = "osrep-conformance"
description = "Differential harness: Rust port vs the C++ oracle"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[[bin]]
name = "dedup_conformance"
path = "src/main.rs"

[dependencies]
osrep-core = { path = "../osrep-core" }
