[package]
name = "osrep-cli"
description = "Omega SREP command-line front end (Rust port)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[[bin]]
name = "osrep"
path = "src/main.rs"

[dependencies]
osrep-core = { path = "../osrep-core" }
